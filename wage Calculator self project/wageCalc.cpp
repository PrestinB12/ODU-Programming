/*
    Example of Encapulation OOP Concept in C++

    By Prestin Bell
*/

#include "wageCalc.h"
// # include <math>
#include <iostream>

using namespace std;

float salary = 50000;
float overtime;
int rate;

totalSalary(salary, overtime, rate)
{
    return totalSalary + (pvertime * rate)
}

employee()
{
    float salary = 50000;
    float overtime;
    int rate;

    totalSalary()
    {
        return this.salary + (this.overtime * this.rate);
    }
} 
employee.salary();